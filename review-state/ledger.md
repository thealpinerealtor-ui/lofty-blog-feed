# Review Engine Ledger
# One line per client: sha256(lowercased client email) | date_day0_sent (or "dry") | date_nudge_sent (or "-") | status
# PRIVACY: no raw names, emails, or phone numbers — hashes only. This repo is public.
